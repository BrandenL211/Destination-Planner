import { updateUI} from '../src/client/js/updateUI';

describe('Get function to be tested', () => {

    test('Checking that the performAction function is defined', () => {
        expect(updateUI).toBeDefined();
    });
});